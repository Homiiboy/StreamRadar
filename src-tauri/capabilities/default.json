{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "StreamRadar desktop permissions for core APIs, safe external URL opening and persisted window state.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "window-state:default"
  ]
}
